---
description: Generate a TIPST flowchart document from a structured JSON protocol
user_invocable: true
---

Generate a TIPST flowchart decision-making document for: $ARGUMENTS

Use the flowchart-generator agent to read the JSON protocol and produce a clear decision-tree flowchart document in `output/flowcharts/`.

If no specific protocol is mentioned, generate flowcharts for all protocols that don't yet have corresponding output files.
